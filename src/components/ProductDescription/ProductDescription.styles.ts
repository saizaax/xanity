import { css } from "../../theme"

export const styles = css({
  display: "flex",
  flexDirection: "column",
  rowGap: "15px",

  h2: {
    fontSize: "20px",
    fontWeight: 700,
    color: "$dark100",
  },

  p: {
    fontSize: "16px",
    fontWeight: 500,
    color: "$dark75",
  }
})