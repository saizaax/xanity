import { css, styled } from "../../theme"

export const styles = css({
  display: "flex",
  columnGap: "60px",
  alignItems: "center",
})

export const NavigationContainer = styled("div", {
  display: "flex",
  columnGap: "40px",
})
